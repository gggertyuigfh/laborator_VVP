#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
    int i, K, Res, N, S, ka, kb, k, d, A, B, C;
    
    cout << "1. С начала суток прошло N секунд (N — целое). Найти количество секунд, прошедших с начала последней минуты." << endl;
    cout << "Введите время в секундах - ";
    cin >> i;
    i = i%60;
    cout << i << " сек"<< endl;
    
    cout << "2. Дни недели пронумерованы следующим образом: 0 — воскресенье, 1 — понедельник, 2 — вторник, . . . , 6 — суббота. Дано целое число K, лежащее в диапазоне 1–365. Определить номер дня недели для K-го дня года, если известно, что в этом году 1 января было понедельником." << endl;
    cout << "Вам нужно ввести номер дня в году (K) - ";
    cin >> K;
    if ((K > 365) or (K < 1))
        {
            cout << "некорректная дата";
        }
    else
        {
        Res = K % 7;
        cout << "Номер дня недели: " << Res << endl;
        }
        
    cout << "3. Дни недели пронумерованы следующим образом: 1 — понедельник, 2 — вторник, . . . , 6 — суббота, 7 — воскресенье. Дано целое число K, лежащее в диапазоне 1–365, и целое число N, лежащее в диапазоне 1–7. Определить номер дня недели для K-го дня года, если известно, что в этом году 1 января было днем недели с номером N"<< endl;
    cout << "Введи K - ";
    cin >> K;
    cout << "Введи N - ";
    cin >> N;
    if ((K > 365) or (K < 1) or (N > 7) or (N < 0))
        {
            cout << "некорректная дата";
        }
    else
        {
        Res = (K + N - 1) % 7;
        cout << "Номер дня недели: " << Res << endl;
        }
        
    cout << "4. Даны целые положительные числа A, B, C. На прямоугольнике размера A × B размещено максимально возможное количество квадратов со стороной C (без наложений). Найти количество квадратов, размещенных на прямоугольнике, а также площадь незанятой части прямоугольника. "<< endl;
    cout << "Введи A: ";
    cin >> A;
    cout << "Введи B: ";
    cin >> B;
    cout << "Введи C: ";
    cin >> C;
    if ((A < 0) or (B < 0) or (C < 0))
        {
            cout << "некорректная дата";
        }
    else
        {
            ka = 0;
            d = C;
            while (d <= A) 
            {
                d = d + C;
                ka = ka + 1;
            }
            kb = 0; d = C;
            while (d <= B)
            {
                d = d + C;
                kb = kb + 1;
            }
        k= ka * kb;
        N = (C * C)*k;
        S = (A * B) - N;

        cout << "Количество квадратов: " << k << endl;
        cout << "Площадь незанятой части: " << S << endl << endl;
        }
        
    cout << "5. Дан номер некоторого года (целое положительное число). Определить соответствующий ему номер столетия, учитывая, что, к примеру, началом 20 столетия был 1901 год"<< endl;
    cout << "Введи год: ";
        cin >> i;
        if (i < 0)
        {
            cout << "некорректная дата";
        }
        else
        {
            Res = i / 100 + 1;
            if (i % 100 == 0) { Res--; }
            cout << "Век: " << Res << endl;
        }
}
