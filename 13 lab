#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
    double x, x1, a, y, s, n, sum;
    int i, N;
    
    cout << "1. Дано вещественное число — цена 1 кг конфет. Вывести стоимость 0.1, 0.2, . . . , 1 кг конфет."<< endl;
    cout << "Цена за киллограм = ";
    cin >> x;
    for (int i = 0; i < 10; i++)
    {
        a += 0.1;
        x1 = x * a;
        cout << a << "кг =" << x1 << endl;
    }
    
    
    cout << "2. Дано целое число N (> 0). Найти произведение 1.1 · 1.2 · 1.3 · . . . (N сомножителей)."<< endl;
    cout << "N = ";
    cin >> i;
    x1 = 1.1;
    for (a = 1; a <= i; a++)
    {
        x *= x1;
        x1 += 0.1;
    }
    cout <<"Произведение = "<< x << endl;
    
    
    cout << "3. Дано целое число N (> 0). Найти квадрат данного числа, используя для его вычисления следующую формулу: N2 = 1 + 3 + 5 + . . . + (2·N − 1). После добавления к сумме каждого слагаемого выводить текущее значение суммы"<< endl;
    cout << "Целое чило N (> 0) = ";
    cin >> x;
    for (int i = 1; i <= x; i++)
    {
        s += (2 * i - 1);
    }
    cout << "Квадрат числа = "<< s << endl;
    
    
    cout << "4. Дано вещественное число A и целое число N (> 0). Используя один цикл, найти сумму 1 + A + A2 + A3 + . . . + AN"<< endl;
    cout << "Введите А - ";
    cin >> a;
    cout << "Введите N - ";
    cin >> n;
    for (int i = 0; i <= n; i++)
    {
        sum += pow(a, i);
    }
    cout << "Сумма = "<< sum << endl;
    
    
    cout << "5. Дано вещественное число A и целое число N (> 0). Используя один цикл, найти значение выражения"<< endl;
    cout << "1 − A + A2 − A3 + . . . ± AN ."<< endl;
    cout << "Условный оператор не использовать."<< endl;
    cout << "Введите А = ";
    cin >> a;
    cout << "Введите N = ";
    cin >> N;
    for (int i = 0; i <= n; i++)
    {
        sum += pow(-a, i);
    }
    cout << "Значение выражения = "<< sum << endl;
}
